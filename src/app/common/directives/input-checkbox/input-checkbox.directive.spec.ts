import { InputCheckboxDirective } from './input-checkbox.directive';

describe('InputCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
