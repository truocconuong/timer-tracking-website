import { InputSwitchDirective } from './input-switch.directive';

describe('InputSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
